export enum Language {
  en = "en",
  es = "es",
}
export enum OptionsForm {
  name = "name",
  email = "email",
  message = "message",
}
export enum Theme {
  dark = "darkTheme",
  light = "lightTheme",
}
export enum Sections{
  home = "home",
  about = "about",
  projects = "projects",
  contact = "contact"
}